{
  "name": "eko-corporate",
  "version": "1.0.0",
  "private": true,
  "description": "Eko Corporate — plateforme de mise en relation agricole & logistique",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node -e \"require('dotenv').config(); const pool=require('./db/pool'); require('./db/seed').assurerDonneesInitiales(pool).then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
